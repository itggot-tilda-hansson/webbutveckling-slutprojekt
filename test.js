function test (event){

    let test = document.querySelector('.dropdown')
    test.classList.toggle('aktiv')

    // let test = document.querySelector("ul")
    // menu.classList.toggle("activate")
}